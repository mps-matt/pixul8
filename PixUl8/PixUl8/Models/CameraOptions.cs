using System;
namespace PixUl8.Models
{
    /// <summary>
    /// Camera options enum - Just used so we don't have to use true/false!
    /// </summary>
    public enum CameraOptions
    {
        Rear,
        Front
    }
}
